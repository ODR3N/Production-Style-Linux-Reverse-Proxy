# Production-Style-Linux-Reverse-Proxy
Built a 3-node Linux lab to simulate a production-style web service delivery path. Configured a Python backend as a systemd-managed service, exposed it through Nginx reverse proxy, implemented TLS termination, request rate limiting, firewall restrictions, SSH hardening, and multi-layer logging for traffic and service troubleshooting.
